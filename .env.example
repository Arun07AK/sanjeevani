# Copy to .env and fill in. .env is gitignored — never commit keys.
OPENAI_API_KEY=sk-...
# Optional: authentic Telugu voice via Sarvam Bulbul TTS (free tier). Falls back to OpenAI TTS if unset.
SARVAM_API_KEY=
